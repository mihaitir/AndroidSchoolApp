<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pdm">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon_app"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ClasamentActivity"></activity>
        <activity android:name=".ScoreActivity" />
        <activity android:name=".SignInActivity" />
        <activity android:name=".RezultatActivity" />
        <activity android:name=".IntrebareActivity" />
        <activity android:name=".StartTestActivity" />
        <activity android:name=".AdaugareIntrebareActivity" />
        <activity android:name=".AdaugaTestActivity" />
        <activity android:name=".StudentActivity" />
        <activity android:name=".ProfesorActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>